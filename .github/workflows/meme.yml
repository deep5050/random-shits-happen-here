name: "Greet With A Random Meme"
on:
  issues:
    types: [opened, reopened]

jobs:
  test:
    name: setup environment
    runs-on: ubuntu-latest
    steps:
      - name: memes on isssues
        uses: deep5050/memes-on-issues-action@issue-1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          allow_owner: true
